var five = require("../");
var board = new five.Board();

board.on("ready", function() {
  var knock = new five.Sensor("A0");

  knock.on("change", function() {
    if (this.value >= 600) {
      console.log("Knock!");
    }
  });
});
